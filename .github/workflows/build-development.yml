name: Build Development Artifacts

# Produces signed + notarized macOS DMG and Windows zip from the development
# branch without creating a tag or GitHub Release. Triggers when this workflow
# file itself is added/edited on development (one-shot), or manually via
# workflow_dispatch (requires the file to be present on the default branch).
#
# Artifacts are uploaded to the Actions run page — download via the web UI or
# `gh run download <run-id>`.
on:
  push:
    branches: [development]
    paths:
      - '.github/workflows/build-development.yml'
  workflow_dispatch:
    inputs:
      version_suffix:
        description: 'Optional version suffix (appended to "dev-<sha>"); leave blank for just the SHA.'
        required: false
        default: ''

permissions:
  contents: read

concurrency:
  group: build-development-${{ github.ref }}
  cancel-in-progress: true

env:
  CMAKE_BUILD_PARALLEL_LEVEL: 4
  VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}\vcpkg-bincache

jobs:
  build-macos:
    # Pinned runner label (not macos-latest) so the Xcode/signing toolchain
    # only changes when we choose to move it.
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Derive development version
        id: version
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          SUFFIX="${{ inputs.version_suffix }}"
          if [ -n "$SUFFIX" ]; then
            VERSION="dev-${SHORT_SHA}-${SUFFIX}"
          else
            VERSION="dev-${SHORT_SHA}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Derived dev version: $VERSION (branch: ${GITHUB_REF_NAME})"

      - name: Cache Homebrew downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/Library/Caches/Homebrew/downloads
            ~/Library/Caches/Homebrew/api
          key: brew-macos-${{ hashFiles('.github/workflows/build-development.yml') }}
          restore-keys: brew-macos-

      - name: Install dependencies
        run: brew install qt opus hidapi openssl@3 dbus brotli libusb

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696

      - name: Configure CMake
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt)" \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
            -DQK4_VERSION_FULL="${{ steps.version.outputs.version }}"

      - name: Build
        run: cmake --build build --config Release

      - name: Create app bundle with dependencies
        run: |
          set -e

          APP_BUNDLE="build/QK4.app"
          FRAMEWORKS="$APP_BUNDLE/Contents/Frameworks"

          # Run macdeployqt to bundle Qt frameworks
          $(brew --prefix qt)/bin/macdeployqt "$APP_BUNDLE" -verbose=2

          # Copy QtDBus.framework (transitive dependency of QtGui in Homebrew Qt)
          echo "=== Copying QtDBus.framework ==="
          QTBASE_LIB="$(brew --prefix qtbase)/lib"
          cp -R "$QTBASE_LIB/QtDBus.framework" "$FRAMEWORKS/"

          # Copy libdbus (dependency of QtDBus)
          echo "=== Copying libdbus ==="
          DBUS_LIB="$(brew --prefix dbus)/lib"
          cp "$DBUS_LIB/libdbus-1.3.dylib" "$FRAMEWORKS/"
          ln -sf libdbus-1.3.dylib "$FRAMEWORKS/libdbus-1.dylib"

          # Copy brotli libraries (dependency of QtNetwork)
          echo "=== Copying brotli libraries ==="
          BROTLI_LIB="$(brew --prefix brotli)/lib"
          cp "$BROTLI_LIB/libbrotlicommon.1.dylib" "$FRAMEWORKS/"
          cp "$BROTLI_LIB/libbrotlidec.1.dylib" "$FRAMEWORKS/"
          cp "$BROTLI_LIB/libbrotlienc.1.dylib" "$FRAMEWORKS/"
          ln -sf libbrotlicommon.1.dylib "$FRAMEWORKS/libbrotlicommon.dylib"
          ln -sf libbrotlidec.1.dylib "$FRAMEWORKS/libbrotlidec.dylib"
          ln -sf libbrotlienc.1.dylib "$FRAMEWORKS/libbrotlienc.dylib"

          # Get OpenSSL paths (resolve symlinks for install_name_tool)
          OPENSSL_LIB="$(brew --prefix openssl@3)/lib"
          OPENSSL_REAL="$(realpath $OPENSSL_LIB)"

          # Copy OpenSSL libraries
          echo "=== Copying OpenSSL libraries ==="
          cp "$OPENSSL_LIB/libssl.3.dylib" "$FRAMEWORKS/"
          cp "$OPENSSL_LIB/libcrypto.3.dylib" "$FRAMEWORKS/"

          # Copy Opus library
          echo "=== Copying Opus library ==="
          OPUS_LIB="$(brew --prefix opus)/lib"
          cp "$OPUS_LIB/libopus.0.dylib" "$FRAMEWORKS/"
          ln -sf libopus.0.dylib "$FRAMEWORKS/libopus.dylib"

          # Copy HIDAPI library
          echo "=== Copying HIDAPI library ==="
          HIDAPI_LIB="$(brew --prefix hidapi)/lib"
          cp "$HIDAPI_LIB/libhidapi.0.dylib" "$FRAMEWORKS/"
          ln -sf libhidapi.0.dylib "$FRAMEWORKS/libhidapi.dylib"

          # Copy libusb library
          echo "=== Copying libusb library ==="
          LIBUSB_LIB="$(brew --prefix libusb)/lib"
          cp "$LIBUSB_LIB/libusb-1.0.0.dylib" "$FRAMEWORKS/"
          ln -sf libusb-1.0.0.dylib "$FRAMEWORKS/libusb-1.0.dylib"

          # Fix permissions (Homebrew files may be read-only)
          chmod -R u+rw "$FRAMEWORKS/"

          # Fix OpenSSL library paths
          echo "=== Fixing library paths ==="
          install_name_tool -id "@executable_path/../Frameworks/libssl.3.dylib" \
            "$FRAMEWORKS/libssl.3.dylib"
          install_name_tool -id "@executable_path/../Frameworks/libcrypto.3.dylib" \
            "$FRAMEWORKS/libcrypto.3.dylib"
          install_name_tool -change "$OPENSSL_REAL/libcrypto.3.dylib" \
            "@executable_path/../Frameworks/libcrypto.3.dylib" \
            "$FRAMEWORKS/libssl.3.dylib"

          # Fix Opus library path
          install_name_tool -id "@executable_path/../Frameworks/libopus.0.dylib" \
            "$FRAMEWORKS/libopus.0.dylib" || true

          # Fix HIDAPI library path
          install_name_tool -id "@executable_path/../Frameworks/libhidapi.0.dylib" \
            "$FRAMEWORKS/libhidapi.0.dylib" || true

          # Fix libusb library path
          install_name_tool -id "@executable_path/../Frameworks/libusb-1.0.0.dylib" \
            "$FRAMEWORKS/libusb-1.0.0.dylib" || true

          # Fix libdbus library path
          install_name_tool -id "@executable_path/../Frameworks/libdbus-1.3.dylib" \
            "$FRAMEWORKS/libdbus-1.3.dylib" || true

          # Fix brotli library paths
          install_name_tool -id "@executable_path/../Frameworks/libbrotlicommon.1.dylib" \
            "$FRAMEWORKS/libbrotlicommon.1.dylib" || true
          install_name_tool -id "@executable_path/../Frameworks/libbrotlidec.1.dylib" \
            "$FRAMEWORKS/libbrotlidec.1.dylib" || true
          install_name_tool -id "@executable_path/../Frameworks/libbrotlienc.1.dylib" \
            "$FRAMEWORKS/libbrotlienc.1.dylib" || true

          # Fix brotlidec's reference to brotlicommon
          BROTLI_COMMON_ORIG=$(otool -L "$FRAMEWORKS/libbrotlidec.1.dylib" | grep brotlicommon | awk '{print $1}')
          if [ -n "$BROTLI_COMMON_ORIG" ]; then
            install_name_tool -change "$BROTLI_COMMON_ORIG" \
              "@executable_path/../Frameworks/libbrotlicommon.1.dylib" \
              "$FRAMEWORKS/libbrotlidec.1.dylib"
          fi

          # Fix brotlienc's reference to brotlicommon
          BROTLI_COMMON_ORIG=$(otool -L "$FRAMEWORKS/libbrotlienc.1.dylib" | grep brotlicommon | awk '{print $1}')
          if [ -n "$BROTLI_COMMON_ORIG" ]; then
            install_name_tool -change "$BROTLI_COMMON_ORIG" \
              "@executable_path/../Frameworks/libbrotlicommon.1.dylib" \
              "$FRAMEWORKS/libbrotlienc.1.dylib"
          fi

          # Fix QtDBus.framework paths
          echo "=== Fixing QtDBus.framework paths ==="
          QTDBUS_BINARY="$FRAMEWORKS/QtDBus.framework/Versions/A/QtDBus"
          if [ -f "$QTDBUS_BINARY" ]; then
            install_name_tool -id "@executable_path/../Frameworks/QtDBus.framework/Versions/A/QtDBus" \
              "$QTDBUS_BINARY"

            DBUS_ORIG=$(otool -L "$QTDBUS_BINARY" | grep libdbus | awk '{print $1}')
            if [ -n "$DBUS_ORIG" ]; then
              install_name_tool -change "$DBUS_ORIG" \
                "@executable_path/../Frameworks/libdbus-1.3.dylib" \
                "$QTDBUS_BINARY"
            fi

            QTGUI_BINARY="$FRAMEWORKS/QtGui.framework/Versions/A/QtGui"
            if [ -f "$QTGUI_BINARY" ]; then
              QTDBUS_ORIG=$(otool -L "$QTGUI_BINARY" | grep QtDBus | awk '{print $1}')
              if [ -n "$QTDBUS_ORIG" ]; then
                install_name_tool -change "$QTDBUS_ORIG" \
                  "@executable_path/../Frameworks/QtDBus.framework/Versions/A/QtDBus" \
                  "$QTGUI_BINARY"
              fi
            fi
          else
            echo "ERROR: QtDBus binary not found at $QTDBUS_BINARY"
            exit 1
          fi

      - name: Import Code Signing Certificate
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/signing.keychain-db
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          echo "$APPLE_CERTIFICATE_BASE64" | base64 --decode > $RUNNER_TEMP/certificate.p12
          security import $RUNNER_TEMP/certificate.p12 \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -A -t cert -f pkcs12 \
            -k "$KEYCHAIN_PATH"

          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Sign Application
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          APP_BUNDLE="build/QK4.app"

          echo "=== Signing frameworks and libraries ==="
          find "$APP_BUNDLE/Contents/Frameworks" -name "*.dylib" -exec \
            codesign --force --options runtime --sign "Developer ID Application: Michael Richard Garcia ($APPLE_TEAM_ID)" {} \;

          find "$APP_BUNDLE/Contents/Frameworks" -name "*.framework" -exec \
            codesign --force --options runtime --sign "Developer ID Application: Michael Richard Garcia ($APPLE_TEAM_ID)" {} \;

          echo "=== Signing PlugIns ==="
          find "$APP_BUNDLE/Contents/PlugIns" -name "*.dylib" -exec \
            codesign --force --options runtime --sign "Developer ID Application: Michael Richard Garcia ($APPLE_TEAM_ID)" {} \;

          echo "=== Signing main application with entitlements ==="
          codesign --force --options runtime \
            --entitlements "resources/QK4.entitlements" \
            --sign "Developer ID Application: Michael Richard Garcia ($APPLE_TEAM_ID)" \
            "$APP_BUNDLE"

          codesign --verify --verbose=2 "$APP_BUNDLE"
          spctl --assess --type execute --verbose "$APP_BUNDLE" || echo "Note: spctl may fail before notarization"

      - name: Create and Notarize DMG
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
        run: |
          DMG_NAME="QK4-${{ steps.version.outputs.version }}-macos.dmg"
          APP_BUNDLE="build/QK4.app"

          echo "=== Creating DMG staging directory ==="
          mkdir -p dmg-staging
          ditto "$APP_BUNDLE" dmg-staging/QK4.app
          ln -s /Applications dmg-staging/Applications

          sync
          touch dmg-staging/.metadata_never_index
          sleep 5

          echo "=== Creating DMG ==="
          for attempt in 1 2 3; do
            if hdiutil create -volname "QK4" \
              -srcfolder dmg-staging \
              -ov -format UDZO \
              -nocrossdev \
              "QK4-unsigned.dmg"; then
              break
            fi
            echo "hdiutil attempt $attempt failed, retrying in 5s..."
            sleep 5
          done
          test -f "QK4-unsigned.dmg" || { echo "DMG creation failed after 3 attempts"; exit 1; }

          echo "=== Signing DMG ==="
          codesign --force --sign "Developer ID Application: Michael Richard Garcia ($APPLE_TEAM_ID)" \
            "QK4-unsigned.dmg"
          mv "QK4-unsigned.dmg" "$DMG_NAME"

          mkdir -p ~/private_keys
          echo "$APPLE_API_KEY_BASE64" | base64 --decode > ~/private_keys/AuthKey_$APPLE_API_KEY_ID.p8

          echo "=== Submitting DMG for notarization ==="
          xcrun notarytool submit "$DMG_NAME" \
            --key ~/private_keys/AuthKey_$APPLE_API_KEY_ID.p8 \
            --key-id "$APPLE_API_KEY_ID" \
            --issuer "$APPLE_API_ISSUER_ID" \
            --wait \
            --timeout 30m

          echo "=== Stapling notarization ticket to DMG ==="
          xcrun stapler staple "$DMG_NAME"

          xcrun stapler validate "$DMG_NAME"
          spctl --assess --type open --context context:primary-signature "$DMG_NAME" || echo "spctl check completed"

          echo "=== Development DMG created ==="
          ls -lh "$DMG_NAME"

          rm -rf dmg-staging ~/private_keys

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: QK4-macos-${{ steps.version.outputs.version }}
          path: QK4-${{ steps.version.outputs.version }}-macos.dmg

  build-windows:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Derive development version
        id: version
        shell: bash
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          SUFFIX="${{ inputs.version_suffix }}"
          if [ -n "$SUFFIX" ]; then
            VERSION="dev-${SHORT_SHA}-${SUFFIX}"
          else
            VERSION="dev-${SHORT_SHA}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Derived dev version: $VERSION"

      - name: Cache vcpkg binary packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: vcpkg-bincache
          # Keyed on the manifest so a dependency or baseline bump rebuilds
          # instead of restoring stale binaries.
          key: vcpkg-win-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-win-

      - name: Install Qt
        uses: jurplel/install-qt-action@48d3ad6db93f3627c8ee7a0454bc6f3744f7e730
        with:
          # 6.11 pending: its restructured download repository isn't supported
          # by the installer tooling yet; bump when a release handles it.
          version: '6.10.*'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2022_64'
          modules: 'qtmultimedia qtshadertools qtserialport'
          cache: true

      - name: Setup vcpkg
        # Fetch first so the builtin-baseline commit in vcpkg.json is always
        # present even when the runner image's vcpkg checkout is older.
        run: |
          New-Item -ItemType Directory -Force -Path "$env:VCPKG_DEFAULT_BINARY_CACHE" | Out-Null
          cd C:\vcpkg
          git fetch origin
          .\bootstrap-vcpkg.bat

      - name: Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756
        with:
          arch: x64

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696

      - name: Configure CMake
        run: cmake -B build -G Ninja -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER_LAUNCHER=sccache -DCMAKE_CXX_COMPILER_LAUNCHER=sccache -DQK4_VERSION_FULL="${{ steps.version.outputs.version }}"

      - name: Build
        run: cmake --build build

      - name: Create release package
        run: |
          mkdir QK4-windows
          copy build\QK4.exe QK4-windows\

          windeployqt --release --no-translations --no-compiler-runtime QK4-windows\QK4.exe

          # Bundle the MSVC runtime DLLs app-locally so users never install a
          # redistributable by hand. (windeployqt --compiler-runtime ships the
          # vc_redist installer instead, which defeats the purpose.)
          $crtDir = Get-ChildItem "$env:VCToolsRedistDir\x64" -Directory -Filter "Microsoft.VC1*.CRT" | Select-Object -First 1
          Copy-Item "$($crtDir.FullName)\*.dll" QK4-windows\

          copy build\vcpkg_installed\x64-windows\bin\opus.dll QK4-windows\
          copy build\vcpkg_installed\x64-windows\bin\hidapi.dll QK4-windows\
          copy build\vcpkg_installed\x64-windows\bin\libssl-3-x64.dll QK4-windows\
          copy build\vcpkg_installed\x64-windows\bin\libcrypto-3-x64.dll QK4-windows\
          copy build\vcpkg_installed\x64-windows\bin\libusb-1.0.dll QK4-windows\

          $runtime = Get-ChildItem QK4-windows -Filter "vcruntime140*.dll"
          if (-not $runtime) {
            Write-Host "VC++ runtime DLLs missing from bundle"
            exit 1
          }

          $ZIP_NAME = "QK4-${{ steps.version.outputs.version }}-windows.zip"
          Compress-Archive -Path QK4-windows -DestinationPath $ZIP_NAME

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: QK4-windows-${{ steps.version.outputs.version }}
          path: QK4-${{ steps.version.outputs.version }}-windows.zip
